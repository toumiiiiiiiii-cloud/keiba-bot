# Renderで動かすサーバー（gunicorn）の設定。
# LINEが画像を取りに来たときに、返信の処理中でも同時に対応できるようにする。
workers = 1      # 画像を /tmp に保存しているので、プロセスは1つにする
threads = 8      # 同時に8件まで処理できる
timeout = 120    # 初回のフォントダウンロードに時間がかかっても落ちないように
